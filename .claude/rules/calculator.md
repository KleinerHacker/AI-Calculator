# Calculation

- It MUST be possible to calculate arbitrary numbers
- The "formula" that results from the chosen characters MUST be displayed at all times
- The result MUST be displayed at all times (even if the formula is corrupt)
