# Using the UI

Start the application via `./gradlew run`. The main window opens as a standalone
window and shows the application icon in the title bar and task bar.

At this stage the window is intentionally empty; input controls and the result
display are documented here as soon as they become available.
